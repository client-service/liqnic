# Shared base — no secrets here, safe to commit.
# Environment-specific values go in .env.local / .env.dev / .env.prod (git-ignored).
# Run with: APP_ENV=local pnpm dev:backend  OR  pnpm dev:backend (defaults to local)

STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000
MEDUSA_SERVE_STATIC_URL=http://localhost:9000
