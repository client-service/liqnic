version: 2.1

executors:
  node-executor:
    docker:
      - image: cimg/node:22.6
    working_directory: ~/project

commands:
  check_changes:
    description: "Check if files in a directory changed"
    parameters:
      dir:
        type: string
    steps:
      - run:
          name: Check changes in << parameters.dir >>
          command: |
            git fetch origin $CIRCLE_BRANCH
            if git diff --quiet origin/$CIRCLE_BRANCH HEAD -- ./<< parameters.dir >>; then
              echo "no" > << parameters.dir >>.changed
            else
              echo "yes" > << parameters.dir >>.changed

  build_and_cache_node:
    description: "Install & build Node project with cache"
    parameters:
      dir:
        type: string
      lockfile:
        type: string
      install_cmd:
        type: string
      build_cmd:
        type: string
    steps:
      - restore_cache:
          keys:
            - v1-<< parameters.dir >>-deps-{{ checksum "<< parameters.dir >>/<< parameters.lockfile >>" }}
            - v1-<< parameters.dir >>-deps-
      - run:
          name: Install & build << parameters.dir >>
          command: |
            cd << parameters.dir >>
            << parameters.install_cmd >>
            << parameters.build_cmd >>
      - save_cache:
          paths:
            - ./<< parameters.dir >>/node_modules
          key: v1-<< parameters.dir >>-deps-{{ checksum "<< parameters.dir >>/<< parameters.lockfile >>" }}

jobs:
  detect-changes:
    executor: node-executor
    steps:
      - checkout
      - check_changes:
          dir: backend
      - check_changes:
          dir: dashboard
      - check_changes:
          dir: storefront
      - persist_to_workspace:
          root: .
          paths:
            - backend.changed
            - dashboard.changed
            - storefront.changed

  build-backend:
    executor: node-executor
    steps:
      - attach_workspace:
          at: .
      - build_and_cache_node:
          dir: backend
          lockfile: yarn.lock
          install_cmd: yarn install --frozen-lockfile
          build_cmd: yarn build
      - run:
          name: Docker build backend
          command: |
            if [ "$(cat backend.changed)" = "yes" ]; then
              TAG=backend-0.1.$CIRCLE_BUILD_NUM
              docker pull $DOCKER_USERNAME/liqnic-backend:latest || true
              docker build --cache-from=$DOCKER_USERNAME/liqnic-backend:latest -t $DOCKER_USERNAME/liqnic-backend:$TAG ./backend
              docker push $DOCKER_USERNAME/liqnic-backend:$TAG
              docker tag $DOCKER_USERNAME/liqnic-backend:$TAG $DOCKER_USERNAME/liqnic-backend:latest
              docker push $DOCKER_USERNAME/liqnic-backend:latest
            else
              echo "Backend not changed, skipping Docker build"
            fi

  build-dashboard:
    executor: node-executor
    steps:
      - attach_workspace:
          at: .
      - build_and_cache_node:
          dir: dashboard
          lockfile: package-lock.json
          install_cmd: npm ci
          build_cmd: npm run build
      - run:
          name: Docker build dashboard
          command: |
            if [ "$(cat dashboard.changed)" = "yes" ]; then
              TAG=dashboard-0.1.$CIRCLE_BUILD_NUM
              docker pull $DOCKER_USERNAME/liqnic-dashboard:latest || true
              docker build --cache-from=$DOCKER_USERNAME/liqnic-dashboard:latest \
                --build-arg VITE_MEDUSA_BASE=$VITE_MEDUSA_BASE \
                --build-arg VITE_MEDUSA_BACKEND_URL=$VITE_MEDUSA_BACKEND_URL \
                --build-arg VITE_MEDUSA_STOREFRONT_URL=$VITE_MEDUSA_STOREFRONT_URL \
                -t $DOCKER_USERNAME/liqnic-dashboard:$TAG ./dashboard
              docker push $DOCKER_USERNAME/liqnic-dashboard:$TAG
              docker tag $DOCKER_USERNAME/liqnic-dashboard:$TAG $DOCKER_USERNAME/liqnic-dashboard:latest
              docker push $DOCKER_USERNAME/liqnic-dashboard:latest
            else
              echo "Dashboard not changed, skipping Docker build"
            fi

  build-storefront:
    executor: node-executor
    steps:
      - attach_workspace:
          at: .
      - build_and_cache_node:
          dir: storefront
          lockfile: package-lock.json
          install_cmd: npm ci
          build_cmd: npm run build
      - run:
          name: Docker build storefront
          command: |
            if [ "$(cat storefront.changed)" = "yes" ]; then
              TAG=storefront-0.1.$CIRCLE_BUILD_NUM
              docker pull $DOCKER_USERNAME/liqnic-storefront:latest || true
              docker build --cache-from=$DOCKER_USERNAME/liqnic-storefront:latest \
                --build-arg MEDUSA_BACKEND_URL=$MEDUSA_BACKEND_URL \
                --build-arg NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=$NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY \
                --build-arg NEXT_PUBLIC_BASE_URL=$NEXT_PUBLIC_BASE_URL \
                --build-arg NEXT_PUBLIC_DEFAULT_REGION=$NEXT_PUBLIC_DEFAULT_REGION \
                -t $DOCKER_USERNAME/liqnic-storefront:$TAG ./storefront
              docker push $DOCKER_USERNAME/liqnic-storefront:$TAG
              docker tag $DOCKER_USERNAME/liqnic-storefront:$TAG $DOCKER_USERNAME/liqnic-storefront:latest
              docker push $DOCKER_USERNAME/liqnic-storefront:latest
            else
              echo "Storefront not changed, skipping Docker build"
            fi

  deploy:
    machine:
      image: ubuntu-2004:current
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Install sshpass
          command: sudo apt-get update && sudo apt-get install -y sshpass
      - run:
          name: Deploy changed services
          command: |
            if [ "$CIRCLE_BRANCH" = "development" ]; then
              SSH_PASS=$SSH_PASSWORD_DEV
              SSH_HOST=$SSH_HOST_DEV
            elif [ "$CIRCLE_BRANCH" = "production" ]; then
              SSH_PASS=$SSH_PASSWORD_PROD
              SSH_HOST=$SSH_HOST_PROD
            fi

            sshpass -p $SSH_PASS ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST 'bash -s' \<< 'EOF'
              cd /home/docker/liqnic

              if [ -f backend.changed ] && [ "$(cat backend.changed)" = "yes" ]; then
                echo "Deploying backend..."
                # docker pull $DOCKER_USERNAME/liqnic-backend:latest
                docker compose up -d backend
              fi

              if [ -f dashboard.changed ] && [ "$(cat dashboard.changed)" = "yes" ]; then
                echo "Deploying dashboard..."
                # docker pull $DOCKER_USERNAME/liqnic-dashboard:latest
                docker compose up -d dashboard
              fi
            EOF

workflows:
  build-deploy-dev-prod:
    jobs:
      - detect-changes
      - build-backend:
          requires:
            - detect-changes
      - build-dashboard:
          requires:
            - detect-changes
      - build-storefront:
          requires:
            - detect-changes
      - deploy:
          requires:
            - build-backend
            - build-dashboard
            - build-storefront
